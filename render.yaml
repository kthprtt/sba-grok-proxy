services:
  - type: web
    name: sba-grok-proxy
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GROK_API_KEY
        sync: false
      - key: ODDS_API_KEY
        sync: false
      - key: PORT
        value: 3000
